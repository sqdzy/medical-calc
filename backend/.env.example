# Server Configuration
PORT=8080
ENVIRONMENT=development
LOG_LEVEL=debug

# Database Configuration
DATABASE_URL=postgres://postgres:postgres@localhost:5432/medical_db?sslmode=disable
# Or use individual components:
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=medical_db
# DB_SSLMODE=disable

# Migrations
# Use file://./migrations when running from backend root.
# If you run compiled binary from another folder (e.g. build/), the app will try to auto-resolve migrations.
MIGRATIONS_PATH=file://./migrations

# JWT Configuration (REQUIRED)
JWT_SECRET=your-super-secret-jwt-key-min-32-chars
JWT_ACCESS_EXPIRY_MINUTES=15
JWT_REFRESH_EXPIRY_DAYS=7

# Encryption Key (REQUIRED - exactly 32 bytes for AES-256)
ENCRYPTION_KEY=your-32-byte-encryption-key-here

# External APIs
NCBI_API_KEY=your-ncbi-api-key

# Yandex Foundation Models (optional)
# Auth (pick one):
# - API key: Authorization: Api-Key <key>
# - IAM token: Authorization: Bearer <token>
YANDEX_GPT_API_KEY=
YANDEX_IAM_TOKEN=

# Folder ID is the `b1g...` part in model_uri like: gpt://<folderId>/aliceai/latest
YANDEX_FOLDER_ID=

# Model name inside your folder. Examples: yandexgpt-lite, yandexgpt, aliceai/latest
YANDEX_GPT_MODEL=yandexgpt-lite

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:19006
